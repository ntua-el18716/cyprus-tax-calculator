# TaxApp Frontend

Vite + React frontend for the TaxApp project.

## Local Development

```bash
cd frontend
npm install
npm run dev
```

The dev server runs at `http://localhost:8080` (see [vite.config.ts](vite.config.ts)).

## Docker

The frontend is built and served via Docker as part of Compose. See the root README for full-stack setup.
